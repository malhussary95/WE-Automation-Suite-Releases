# WE ECRM Extractor Professional GUI v2

The main `ecrm_extractor/gui.py` is redesigned to match the approved enterprise dashboard concept while keeping the existing extraction engine in `runner.py` and `dependencies.py`.

Run from the ECRM folder:

`python main.py`

Legacy UI is preserved as `ecrm_extractor/gui_legacy.py`.
